import Template from '../../components/template'

const Propriedades = () => {
    return(
        <Template>
            <p>Painel / Gerenciar propriedades</p>
                        
        </Template>
    )
}

export default Propriedades